import { styled } from "@mui/system";

const HomeContainer = styled('div')({
    backgroundColor : '#123245',
    height : '100vh',
    width : '100%'
})

const Home : React.FC = () =>{
    return (
        <HomeContainer>

        </HomeContainer>
    )
}

export default Home